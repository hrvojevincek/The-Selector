SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://127.0.0.1:5173/auth/callback
SESSION_SECRET=  # openssl rand -hex 32 — required for session cookies

# Optional — mix search
YOUTUBE_API_KEY=           # YouTube Data API v3 — enables YouTube results

# Optional — not used for read-only mix search today (Mixcloud /search/ is public)
MIXCLOUD_CLIENT_ID=
MIXCLOUD_CLIENT_SECRET=

REDIS_URL=
